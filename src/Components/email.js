// email.js
let email = ''; // Initialize email variable

const setEmail = (newEmail) => {
  email = newEmail; // Update email variable
};

export { email, setEmail }; // Export both email variable and setEmail function
